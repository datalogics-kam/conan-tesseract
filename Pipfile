[[source]]
url = "http://artifactory.dlogics.com:8081/artifactory/api/pypi/pypi/simple"
verify_ssl = false
name = "pypi"

[packages]
conan = ">=1.4.0,<2.0"
conan_package_tools = ">=0.17.3,<0.18"
# We once had to patch conan_package_tools for a bug we ran into. The next line
# is a reminder of how to get the package tools from a branch of a git repo.
# conan_package_tools = {git = "ssh://git@github.com/datalogics-kam/conan-package-tools.git", ref = "use-cached-auth"}
invoke = "<0.23,>=0.22.1"
dl-conan-build-tools = "==0.1.1"

[dev-packages]

[requires]
python_version = "3.6"

[pipenv]
allow_prereleases = true
